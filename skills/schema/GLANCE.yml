# schema — schemapedia (multi-family registry)

name: schema
version: 1.15.1
description: |
  Indexed "schema" senses: interchange, notation (YAML Jazz), execution (shell-orchestration), introspection (cursor-mirror DB/model),
  causal (Drescher), frames, K-lines, Society of Mind, registry_meta (mechanism-of-mechanisms), SQL/SQLite, vcs (git), collaboration (GitHub), component_interop (com-xpcom).   Optional per-mechanism SCHEMAPEDIA-*-AUGMENT.yml. systems.yml for non-format systems.
  supersession-suggestions.yml: relation_semantics predicates; why removes/adds/trades (COM/XML/SOAP clusters). blend-space.yml: simplicial complex, barycentric blends, proximity-to-self (generality/simplicity/efficiency/influence).
  Optional cli_tools. Names, delegates, gateways—not one validator.

when_to_use:
  - Choosing or documenting any schema-like mechanism in MOOLLM
  - Relating wire formats, YAML Jazz as registry plugin, DB DDL, Drescher units, or frames without conflating them
  - SQLite/SQL migration and API boundary design at the registry level

related_skills: [schema-mechanism, schema-factory, knowledge-frames, k-lines, society-of-mind, yaml-jazz, cursor-mirror]

pyramid:
  glance: GLANCE.yml
  card: CARD.yml
  skill: SKILL.md

schemas_dir: schemas/
registry: schemas/registry.yml
gateways: schemas/gateways.yml

tags: [schemapedia, registry, interchange, drescher, frames, sql, sqlite, gateways, moollm]

license: MIT
