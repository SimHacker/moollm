# CARD.yml — no-ai-joking (condensed)

card:
  id: no-ai-joking
  name: "No AI Joking"
  type: [skill, constraint, tone]
  emoji: "🤐"
  rarity: common
  tagline: "Suppress jokes, puns, and performative humor"
  description: |
    Enforces a straight tone. Blocks quips and whimsy unless explicitly allowed.

parameters:
  bias:
    type: float
    default: 1.0
    description: "Humor suppression strength (negative values invert)"
  exceptions:
    type: list
    default: []
    description: "Allowed humor types (e.g., puns, callbacks)"
  enterprise_mode:
    type: bool
    default: false
    description: "Strict corporate tone"

rules:
  allow: [clarity, directness, neutral tone]
  avoid: [puns, jokes, whimsical asides, comedic voices]

examples:
  strict: "MOUNT no-ai-joking on BOARD-ROOM --mode afflict"
  allow_puns: "MOUNT no-ai-joking on WRITER params: { exceptions: [puns] }"

references:
  details: "skills/no-ai-joking/SKILL.md"
