# Self-Correction Example Template
# Copy this when logging a new violation

example:
  # When this happened
  timestamp: "YYYY-MM-DDTHH:MM:SSZ"
  
  # Who caught it
  contributor: "user"  # or "llm" for self-catch
  
  # Which cardinal sin was violated
  violation:
    sin: "1-verbosity"  # 1-10
    description: "Brief description"
    
  # The problematic output
  original: |
    [Paste the exact text that violated the rule]
    
  # Why this was wrong
  analysis: |
    This violated [sin] because:
    - [Specific reason]
    - [What it wasted]
    - [What it obscured]
    
  # What should have been said
  correction: |
    [The corrected version — tight, clear, direct]
    
  # The lesson to remember
  lesson: |
    [A memorable principle extracted from this case]
    
  # Optional: context about when this pattern appears
  context:
    situation: "When [context]"
    trigger: "User asked about [thing]"
    
  # Optional: related examples
  see_also:
    - "2026-01-XX-related-example.yml"
